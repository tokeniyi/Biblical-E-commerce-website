# Database connection string for Prisma CLI (migrations, introspection).
# Against Neon: use the UNPOOLED/DIRECT connection string (formerly DIRECT_URL).
# For local Docker Postgres (see docker-compose.yml):
#   postgresql://postgres:postgres@localhost:5432/app_dev
DATABASE_URL=
DIRECT_URL=
